---
engines:
  duplication:
    enabled: false
    config:
      languages:
      - typescript
  eslint:
    enabled: true
    channel: "eslint-3"
  fixme:
    enabled: true
ratings:
  paths:
  - "**.ts"
exclude_paths:
- dist/
- test/
